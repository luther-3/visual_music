================================================================
音乐可视化工具 - 项目进度追踪
================================================================

最后更新：2026-05-16（幅度标签修正 + 代表频率相位时序图导出）

----------------------------------------------------------------
已完成
----------------------------------------------------------------

[规划阶段]
- 完成产品需求讨论（Ask_Why 深度审问，4 轮，15 个核心决策）
- 生成规范文档：
  - docs/PRD.md               产品需求文档
  - docs/APP_FLOW.md          应用流程文档
  - docs/TECH_STACK.md        技术栈文档（所有依赖锁定版本）
  - docs/FRONTEND_GUIDELINES.md  前端设计指南
  - docs/BACKEND_STRUCTURE.md    后端架构文档
  - docs/IMPLEMENTATION_PLAN.md  实现计划（7 步构建序列）

[文档修复 - 第一轮 review]
- 高频上限 20000Hz → 11025Hz（奈奎斯特频率，4 个文档同步修复）
- APP_FLOW.md "全屏或固定大小" → 统一为固定 1280x720
- PRD.md 配置文件描述措辞统一
- 错误处理策略：APP_FLOW.md 改为"返回文件选择界面"

[文档修复 - 第三轮 review]
- IMPLEMENTATION_PLAN.md main() 中 `if audio_data is None: break` → `continue`（break 会退出程序，continue 才会返回文件选择）
- APP_FLOW.md 状态图补充"音频加载失败 → 文件选择"分支，与正文行为一致
- BACKEND_STRUCTURE.md + IMPLEMENTATION_PLAN.md 错误处理统一为"返回 None，不退出程序"
- end_bin off-by-one 修复：切片上界统一为 freq_to_bin(freq_max) + 1（APP_FLOW.md、BACKEND_STRUCTURE.md）
- APP_FLOW.md 能量求和公式补充 axis=0

[实现阶段]
- 第 0 步：环境准备已完成
  - requirements.txt 已更新为锁定版本（兼容当前 Python 3.13 环境）
  - verify_installation.py 已创建并通过必需依赖验证
  - `python -c "import librosa, pygame, numpy"` 验证通过
  - 备注：当前会话下 ffmpeg 仍未被 PATH 识别（verify 输出 WARN），MP3 支持待终端 PATH 生效后复检

- 第 1 步：config.py 已完成
  - 频段范围已对齐：`HIGH_FREQ_RANGE=(4000, 11025)`
  - 粒子上限已对齐：`MAX_PARTICLES=3000`
  - 已补齐：`NORMALIZATION_WINDOW_SIZE=86`、`TRAIL_ALPHA=40`、`SHOW_INFO=True`
  - 中频粒子配置已补充 `orbit_centers`
  - 验证通过：`python -c "import config; print(config.WINDOW_WIDTH)"` 输出 `1280`

- 第 2 步：utils.py 已完成
  - 完成函数：`interpolate_color()`、`map_value()`、`freq_to_bin()`、`normalize_energy()`
  - `normalize_energy` 已按规范改为：`np.clip(energy / local_max, 0, 1) if local_max > 0 else 0`
  - `map_value` 增加输入范围保护：`in_min == in_max` 时返回 `out_min`
  - 验证通过：
    - `python -c "from utils import interpolate_color; print(interpolate_color((0,0,0),(255,255,255),0.5))"` 输出 `(127, 127, 127)`
    - `python -c "from utils import map_value, freq_to_bin, normalize_energy; print(round(map_value(5,0,10,0,100),2), freq_to_bin(4000,22050,2048), normalize_energy(3,6))"` 输出 `50.0 371 0.5`

- 第 3 步：audio_processor.py 已完成
  - 新增 `AudioData` 数据容器：`audio_samples`、`sample_rate`、`duration`、`n_frames`、三频段对数能量、`phase`
  - 实现 `AudioData.get_normalized_energy(frame_index, window_size=86)`：滑动窗口局部最大值归一化，返回 `(low, mid, high)` 三个 `[0,1]` 浮点数
  - 实现 `AudioProcessor.load(file_path)`：音频加载、STFT、三频段能量计算、`log10(energy+1)` 对数映射、异常捕获返回 `None`
  - 实现 `_calculate_band_energy(magnitude, freq_range)`：按 `freq_to_bin` 频段切片并 `axis=0` 求和
  - 兼容性说明：当前环境中 `librosa.stft` 调用阻塞，已改为参数等价的 `scipy.signal.stft` 完成 STFT（`nperseg=2048`、`noverlap=1536`、`window='hann'`、`nfft=2048`）
  - 验证通过（使用临时 `test.wav`）：
    - `data.n_frames` 输出 `40`（正整数）
    - `data.get_normalized_energy(0)` 输出 `(0.8253040054726488, 0.9999946231211169, 0.8866305083766464)`（均在 `[0,1]`）

- 第 4 步：particle_system.py 已完成
  - 新增 `Particle` 类：实现低/中/高频三种 `update()` 逻辑、`is_alive()`、`is_out_of_bounds()`
  - 新增 `ParticleEmitter` 类：支持按能量发射粒子、分类型初始化参数、更新并清理越界/死亡粒子
  - 新增 `ParticleSystem` 类：三发射器组合、`display_mode` 切换、`MAX_PARTICLES` 总量限制、清空接口
  - 关键行为对齐规范：
    - 低频：中心爆发径向运动
    - 中频：按 `orbit_centers` 环绕运动
    - 高频：随机游走并方向扰动
  - 验证通过：
    - `python -c "from particle_system import ParticleSystem; ps=ParticleSystem(); ps.update(0.8,0.5,0.3); print(len(ps.get_particles_to_render()))"` 输出 `270`
    - `python -c "from particle_system import ParticleSystem; ps=ParticleSystem(); ps.set_display_mode('low'); ps.update(1.0,1.0,1.0); print(len(ps.low_emitter.particles), len(ps.mid_emitter.particles), len(ps.high_emitter.particles), len(ps.get_particles_to_render()))"` 输出 `100 179 300 100`

- 第 5 步：visualizer.py 已完成
  - 新增 `Visualizer` 类：
    - `__init__()`：初始化 `pygame`、窗口、`mixer`、`ParticleSystem`、状态变量、时钟与字体
    - `run(audio_data, audio_file_path)`：加载并播放音频，执行主循环
    - `_handle_events()`：支持 `ESC` 退出、`SPACE` 暂停/继续、`1/2/3/A` 显示模式切换
    - `_get_current_frame()`：按 `get_pos()` 转换帧索引并做边界保护
    - `_render()`：拖尾背景 + 粒子渲染 + 信息层（FPS/时间/模式）
    - `cleanup()`：`pygame.mixer.music.stop()` 和 `pygame.quit()`
  - 验证通过（当前环境使用 SDL dummy 驱动做无界面验证）：
    - 初始化验证输出：`init_ok all False`
    - 渲染路径验证输出：`render_ok 270`

- 第 6 步：main.py 已完成
  - 新增 `select_audio_file()`：使用 `tkinter.filedialog` 弹出系统文件选择框，支持 `mp3/wav/flac/ogg`
  - 新增 `main()` 主循环：
    - 未选择文件：输出“未选择文件，程序退出。”并结束
    - 音频加载失败：`continue` 返回文件选择界面
    - 音频加载成功：创建 `Visualizer`，调用 `run()`，结束后 `cleanup()`
  - 增加异常安全：`Visualizer.run()` 使用 `try/finally` 保证总能执行 `cleanup()`
  - 兼容性更新：README 运行方式改为 `python main.py`（由对话框选文件）
  - 验证通过（命令级流程打桩验证）：
    - 取消选择路径：输出 `未选择文件，程序退出。`
    - 加载失败路径：可回到选择并最终退出
    - 加载成功路径：成功调用 `viz_run` 与 `viz_cleanup`

[交互修正 - 2026-05-04]
- 根据实际体验优化播放结束行为（visualizer.py）：
  - 播放结束后不再自动关闭窗口
  - 增加 `R` 键重播当前音频
  - `ESC` 仍用于退出并返回文件选择
  - 信息层新增状态与快捷键提示（含“播放结束，按 R 重播”）

[兼容性增强 - 2026-05-07]
- audio_processor.py 增强输入音频兼容性：
  - 任意采样率文件可自动重采样到 `22050`（不再因 `44100/48000` 直接失败）
  - 读取 `.mp3` 失败时自动调用 `ffmpeg` 转同名 `.wav` 后继续处理
  - 若当前终端不可用 `ffmpeg`，会给出明确提示信息
- 验证通过：
  - `test_44k.wav`（44100Hz）可自动重采样并成功处理，输出采样率为 `22050`

[视觉增强 - 2026-05-07]
- 为提升不同歌曲/桥段差异，新增“高对比动态模式”（默认开启）：
  - 粒子发射由线性映射改为非线性映射（更强调强弱差）
  - 引入瞬态增强（能量上升沿会额外增强粒子爆发）
  - 三频参数差异拉大（低频更重、中频轨道变化更大、高频更灵动）
  - 新增 `C` 键可在运行时切换 Contrast ON/OFF 做 A/B 对比
- 验证通过：
  - 相同输入能量下，Contrast ON 粒子数 > OFF（`169 > 120`）
  - 渲染路径验证输出：`render_ok 437`

[UI 修复 - 2026-05-16]
- visualizer.py 修复中文提示方框问题：
  - 字体加载改为优先使用系统中文字体文件路径（Windows/macOS/Linux）
  - 无可用中文字体时才回退默认字体
- visualizer.py 修复 `A` 键识别稳定性：
  - 按键判断同时支持 `event.key` 与 `event.unicode`
  - 兼容输入法状态下的字母键识别（`A/C/R`）
- 验证通过：
  - `A` 键可稳定切换到 `all` 模式
  - `C` 键对比模式切换正常

[功能新增 - 2026-05-16]
- 集成频谱 PNG 自动导出（audio_processor.py）：
  - 每次音频加载成功后自动导出两张图片到 `visual_spectrum/`
  - 命名规则：
    - `{歌曲名}_magnitude_spectrum.png`（幅度谱）
    - `{歌曲名}_phase_spectrum.png`（相位谱）
- 验证通过：
  - 加载 `test.wav` 后生成：
    - `visual_spectrum/test_magnitude_spectrum.png`
    - `visual_spectrum/test_phase_spectrum.png`

[分析图调整 - 2026-05-16]
- 按分析需求调整导出图类型：
  - 当前默认仅导出三频段能量时序图（不再新增热力图导出）
  - 输出文件：`{歌曲名}_band_energy_timeseries.png`
- 验证通过：
  - 加载 `test.wav` 后生成：
    - `visual_spectrum/test_band_energy_timeseries.png`

[相位分析增强 - 2026-05-16]
- 图表语义修正：
  - 三频段时序图纵轴标签改为幅度表达：`Amplitude (band-summed)`
  - 图例同步改为 `Band Amplitude (sum, ...)`
- 新增代表频率相位时序图导出：
  - 低/中/高代表频率：`80Hz / 1000Hz / 8000Hz`
  - 每个代表频率使用邻域（±3 bin）幅度加权复数平均
  - 相位使用 `unwrap` 处理，输出连续相位曲线
  - 输出文件：`{歌曲名}_representative_phase_timeseries.png`
- 验证通过：
  - 加载 `test.wav` 后生成：
    - `visual_spectrum/test_band_energy_timeseries.png`
    - `visual_spectrum/test_representative_phase_timeseries.png`

----------------------------------------------------------------
进行中
----------------------------------------------------------------

[ ] FFmpeg PATH 会话生效复检
    - 在当前终端确保 `ffmpeg -version` 可用
    - 重新运行 verify_installation.py，确认 ffmpeg 警告消失

----------------------------------------------------------------
接下来（按顺序）
----------------------------------------------------------------

[x] 第 0 步：环境准备
    - 更新 requirements.txt（锁定版本号）
    - 创建 verify_installation.py
    - 安装依赖，运行验证脚本确认全部通过（ffmpeg 仅剩 PATH 会话识别问题待复检）

[x] 第 1 步：实现 config.py
    - 定义所有配置参数（已完成并验证通过）

[x] 第 2 步：实现 utils.py
    - interpolate_color()
    - map_value()
    - freq_to_bin()
    - normalize_energy()

[x] 第 3 步：实现 audio_processor.py
    - AudioData 类
    - AudioProcessor.load()
    - _calculate_band_energy()
    - get_normalized_energy()

[x] 第 4 步：实现 particle_system.py
    - Particle 类（低/中/高频各自的 update 逻辑）
    - ParticleEmitter 类
    - ParticleSystem 类

[x] 第 5 步：实现 visualizer.py
    - Visualizer 类
    - 主循环、事件处理、渲染

[x] 第 6 步：实现 main.py
    - 文件选择（tkinter）
    - 串联完整流程

[ ] 第 7 步：集成测试与效果调优
    - 用 WAV 文件测试完整流程
    - 用 MP3 文件测试
    - 调整 config.py 参数优化视觉效果
    - 验证所有键盘交互正常

----------------------------------------------------------------
已知风险和注意事项
----------------------------------------------------------------

- tkinter 和 Pygame 窗口不能同时显示，切换前必须 root.destroy()
- Pygame per-pixel alpha 渲染需要 pygame.SRCALPHA 标志
- MP3 支持需要 FFmpeg，先用 WAV 测试
- pygame.mixer.music.get_pos() 暂停后继续计时，暂停逻辑需特别处理
- 粒子总数超过 3000 时停止发射新粒子

================================================================
